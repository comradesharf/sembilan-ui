pre-commit:
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,css}"
      run: pnpm dlx @biomejs/biome check --unsafe --write --colors=off {staged_files}
      stage_fixed: true
